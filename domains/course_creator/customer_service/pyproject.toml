[project]
name = "customer-service"
version = "0.1.0"
description = "Course Creator Customer Service Agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-genai",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
